<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar <%= animal.Nombre %> - Biblioteca Animal</title>
    
    <link rel="stylesheet" href="/styles/Home.css">
    <link rel="stylesheet" href="/styles/editarAn.css">
</head>

<body>
    <div class="container fade-in">
        <div class="header-actions">
            <div class="header-titles">
                <h1>✏️ Editando: <%= animal.Nombre %></h1>
                <span class="subtitle">Modifica los datos del animal y guarda los cambios</span>
            </div>
            <a href="/animales/<%= animal.ID %>" class="btn btn-secondary">↩️ Cancelar</a>
        </div>

        <div class="form-container">
            <form action="/animales/editar/<%= animal.ID %>" method="POST" enctype="multipart/form-data">
                
                <input type="hidden" name="imagen_actual" value="<%= animal.Imagen || '' %>">

                <div class="form-row">
                    <div class="form-group">
                        <label for="nombre">Nombre Común *</label>
                        <input type="text" id="nombre" name="nombre" value="<%= animal.Nombre %>" required>
                    </div>

                    <div class="form-group">
                        <label for="nombreCientifico">Nombre Científico *</label>
                        <input type="text" id="nombreCientifico" name="nombreCientifico" 
                               value="<%= animal.NombreCientifico %>" required style="font-style: italic;">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="clasificacion">Clasificación *</label>
                        <select id="clasificacion" name="clasificacion_id" required>
                            <% clasificaciones.forEach(c => { %>
                                <option value="<%= c.ID %>" <%= animal.Clasificacion_ID === c.ID ? 'selected' : '' %>>
                                    <%= c.Tipo %> - <%= c.Subtipo %>
                                </option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="habitat">Hábitat *</label>
                        <select id="habitat" name="habitat_id" required>
                            <% habitats.forEach(h => { %>
                                <option value="<%= h.ID %>" <%= animal.Habitat_ID === h.ID ? 'selected' : '' %>>
                                    <%= h.Tipo %> - <%= h.Subtipo %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="clima">Clima Preferido</label>
                        <select id="clima" name="clima_id" required>
                            <% climas.forEach(c => { %>
                                <option value="<%= c.ID %>" <%= animal.Clima_ID === c.ID ? 'selected' : '' %>>
                                    <%= c.Clima %>
                                </option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="alimentacion">Tipo de Alimentación</label>
                        <select id="alimentacion" name="alimentacion_id" required>
                            <% alimentaciones.forEach(a => { %>
                                <option value="<%= a.ID %>" <%= animal.Alimentacion_ID === a.ID ? 'selected' : '' %>>
                                    <%= a.Tipo %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="reproduccion">Tipo de Reproducción</label>
                        <select id="reproduccion" name="reproduccion_id" required>
                            <% reproducciones.forEach(r => { %>
                                <option value="<%= r.ID %>" <%= animal.Reproduccion_ID === r.ID ? 'selected' : '' %>>
                                    <%= r.Tipo %>
                                </option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="form-group">
                        <label>Tiempo de Vida (Años)</label>
                        <div class="vida-inputs">
                            <input type="number" name="tiempoVidaMin" placeholder="Mín" 
                                   value="<%= animal.TiempoVidaMin %>" min="0" step="0.1">
                            <span class="separator">-</span>
                            <input type="number" name="tiempoVidaMax" placeholder="Máx" 
                                   value="<%= animal.TiempoVidaMax %>" min="0" step="0.1">
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label for="descripcion">Descripción</label>
                    <textarea id="descripcion" name="descripcion" rows="5"><%= animal.Descripcion %></textarea>
                </div>

                <div class="image-edit-section">
                    <div class="current-image">
                        <label>Imagen Actual:</label>
                        <% if (animal.Imagen) { %>
                            <div class="img-preview-box">
                                <img src="/images/animales/<%= animal.Imagen %>" alt="Actual">
                            </div>
                        <% } else { %>
                            <div class="no-img-box">
                                <span>Sin imagen</span>
                            </div>
                        <% } %>
                    </div>

                    <div class="new-image-upload">
                        <label for="imagen">Cambiar Imagen (Opcional)</label>
                        <input type="file" id="imagen" name="imagen" accept="image/*">
                        <small>Deja este campo vacío si quieres mantener la imagen actual.</small>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-save">💾 Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>