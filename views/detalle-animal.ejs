<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= animal.Nombre %> - Biblioteca Animal</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/Home.css">
    <link rel="stylesheet" href="/styles/detalle-animal.css">
</head>

<body>
    <div class="container fade-in">
        <div class="top-nav">
            <a href="/" class="btn-back">
                <span>⬅️</span> Volver al Listado
            </a>
            <div class="admin-actions">
                <a href="/animales/editar/<%= animal.ID %>" class="btn btn-edit">✏️ Editar</a>
                <form action="/animales/eliminar/<%= animal.ID %>" method="POST" onsubmit="return confirm('¿Estás seguro de eliminar a <%= animal.Nombre %>? Esta acción no se puede deshacer.')">
                    <button type="submit" class="btn btn-delete">🗑️ Eliminar</button>
                </form>
            </div>
        </div>

        <div class="detail-card">
            
            <div class="detail-header">
                <div class="image-container">
                    <% if (animal.Imagen) { %>
                        <img src="/images/animales/<%= animal.Imagen %>" alt="<%= animal.Nombre %>">
                    <% } else { %>
                        <div class="no-image-placeholder">
                            <span>🐾</span>
                        </div>
                    <% } %>
                </div>
                
                <div class="title-section">
                    <span class="classification-badge"><%= animal.TipoClasificacion %> › <%= animal.SubtipoClasificacion %></span>
                    <h1><%= animal.Nombre %></h1>
                    <h2 class="scientific-name"><%= animal.NombreCientifico %></h2>
                    
                    <div class="quick-tags">
                        <span class="tag diet-tag">🍽️ <%= animal.TipoAlimentacion %></span>
                        <span class="tag reproduction-tag">❤️ <%= animal.TipoReproduccion %></span>
                    </div>
                </div>
            </div>

            <hr class="divider">

            <div class="detail-body">
                
                <div class="description-section">
                    <h3>📖 Descripción</h3>
                    <p><%= animal.Descripcion ? animal.Descripcion : 'No hay descripción disponible para este animal.' %></p>
                </div>

                <div class="info-grid-section">
                    <h3>🌍 Datos del Hábitat</h3>
                    
                    <div class="info-card">
                        <div class="icon">🌿</div>
                        <div class="data">
                            <label>Hábitat</label>
                            <span><%= animal.TipoHabitat %> (<%= animal.SubtipoHabitat %>)</span>
                        </div>
                    </div>

                    <div class="info-card">
                        <div class="icon">c</div>
                        <div class="data">
                            <label>Clima Preferido</label>
                            <span><%= animal.Clima %></span>
                        </div>
                    </div>

                    <div class="info-card">
                        <div class="icon">⏳</div>
                        <div class="data">
                            <label>Esperanza de Vida</label>
                            <span>
                                <% if (animal.TiempoVidaMin && animal.TiempoVidaMax) { %>
                                    De <%= animal.TiempoVidaMin %> a <%= animal.TiempoVidaMax %> años
                                <% } else if (animal.TiempoVidaMin) { %>
                                    Desde <%= animal.TiempoVidaMin %> años
                                <% } else { %>
                                    Desconocido
                                <% } %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>